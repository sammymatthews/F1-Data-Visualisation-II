/**
 * Repeat a string a specified number of times.
 */
export default function repeat(str, reps) {
    let s = '';
    while (--reps >= 0)
        s += str;
    return s;
}
//# sourceMappingURL=repeat.js.map